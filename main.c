#include <stdio.h>

/* /pso <plik_mapy> -p <liczba_czastek> -i <liczba_iteracji> -c
<plik_konfiguracyjny z parametrami_PSO> -n <co_ktora_iteracje_zapis_postepow> */
int main(int argc, char *argv[]) { FILE *map = argc > 1 ? return 0; }
